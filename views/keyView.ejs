<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload - MyDrive</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('./partials/sidebar', { headerTitle: "Current Keys", folders: folders || []}) %>
    <main class="content">
        <div class="content-header">
            <%- include('./partials/header', { headerTitle: "Current Keys", user: user || {} }) %>
        </div>
        
        <div class="form-container">
            <div class="notice warning" style="margin-bottom: 1em; color: #b85c00; background: #fffbe6; padding: 0.75em 1em; border-radius: 4px;">
                <strong>Notice:</strong> Your encryption keys are stored in the browser cache and are not permanent. Please copy and save them securely if you want to access your files later.
            </div>
            <div class="key-container">
                <div id="encryption-key"></div>
                <svg id="copy-key-btn" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-copy" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M4 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2zm2-1a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1zM2 5a1 1 0 0 0-1 1v8a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1v-1h1v1a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h1v1z"/>
                </svg>
            </div>
        </div>
    </main>
    <script src="/js/getKeys.js"></script>
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        const copyBtn = document.getElementById('copy-key-btn');
        const keyDiv = document.getElementById('encryption-key');
        // Show button only if there is a key
        if (keyDiv.textContent.trim()) {
            copyBtn.style.display = '';
        } else {
            copyBtn.style.display = 'none';
        }
        copyBtn.addEventListener('click', async function() {
            const key = keyDiv.textContent.trim();
            if (key) {
                await navigator.clipboard.writeText(key);
            }
        });
    });
    </script>
</body>
</html>
