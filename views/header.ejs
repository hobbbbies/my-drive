<header class="header">
    <% if (locals.user) { %>
        <h2>Welcome, <%= user.name%></h2>
    <% } %>
    <div class="bottom-header">
        <h1><%= headerTitle %></h1>
        <p>MyDrive</p>
        <a href="/upload" class="upload-btn">Upload file</a>
    </div>
</header>